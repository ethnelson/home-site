{
  "name": "home_site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage && cd client && npm run test",
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build"
  },
  "keywords": [],
  "author": "Ethan Nelson",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "engines": {
    "node": "16.9.1"
  },
  "devDependencies": {
    "jest": "^27.4.5"
  },
  "jest": {
    "modulePathIgnorePatterns": ["client"]
  }
}
